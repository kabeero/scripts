section: options
	#witchCorners = top-left, top-right, bottom-left, bottom-right
	switchCorners = all
	switchCornerSize = 20

	#keystroke(alt+shift+left) = switchInDirection(left)
	#keystroke(alt+shift+right) = switchInDirection(right)
	#keystroke(super+f1) = switchToScreen(left-screen)
	#keystroke(super+f2) = switchToScreen(main-screen)
end

section: screens

	host:
		shift = shift
		ctrl  = super
		alt   = alt
		meta  = meta
		super = ctrl

	client:
		shift = shift
		ctrl  = ctrl
		alt   = meta
		meta  = alt
		super = ctrl

end

section: links

	client:
		left=host

	host:
		right=client

end
